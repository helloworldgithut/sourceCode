<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sendi.dao.NumberDataDaoI">
    <insert id="addData" parameterType="com.sendi.entity.NumberData">
      insert into number_data (dev_id, res_id, valued, send_time, unit, pro_id)
        values (#{devId}, #{resId}, #{valued},#{sendTime},#{unit}, #{proId})
    </insert>

</mapper>
